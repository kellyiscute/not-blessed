{
  "name": "not-blessed",
  "version": "1.0.0",
  "description": "A not-blessed tui framework that saves you an afternoon looking at blessed's api document",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc -p . --outDir build/",
    "test:m": "yarn tsc && node build/test/mouse.js",
    "test:kb": "yarn tsc && node build/test/keyboard.js"
  },
  "author": "Kelly",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^16.9.4",
    "typescript": "^4.4.3"
  }
}
